package tw.neil.sample.arch.mvvm.scheduleonoff

object Constant {
    const val ACTION_ALARM = "${BuildConfig.APPLICATION_ID}.action.ALARM"
    const val EXTRA_SCREEN_STATE = "${BuildConfig.APPLICATION_ID}.extra.SCREEN_STATE"
    const val EXTRA_ID = "${BuildConfig.APPLICATION_ID}.extra.ID"
}